<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- List View -->
        <record model="ir.ui.view" id="asset_location_tree">
            <field name="name">account.asset.location.tree</field>
            <field name="model">account.asset.location</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                </tree>
            </field>
        </record>
        
        <!-- Form View -->
        <record model="ir.ui.view" id="asset_location_form">
            <field name="name">account.asset.location.form</field>
            <field name="model">account.asset.location</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record model="ir.actions.act_window" id="asset_location_action">
            <field name="name">Asset Location</field>
            <field name="res_model">account.asset.location</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem 
            id="menu_asset_location"
            name="Asset Location"
            parent="account.account_account_menu"
            sequence="10"
            action="ms_account_asset.asset_location_action"
        />

    </data>
</odoo>