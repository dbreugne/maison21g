<odoo>
    <record id="account_task_form" model="ir.ui.view">
        <field name="name">Project Task</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook/page[@name='extra_info']/group" position="inside">
               <group string="Developer Estimation">
                    <field name="hours" invisible="1"/>
                    <field name="est_hours" widget="float_time"/>
                    <field name="amount"/>
                    <field name="total_amount" invisible="1"/>
               </group>
            </xpath>
        </field>
    </record>

    <record id="account_task_tree_view" model="ir.ui.view">
        <field name="name">project.task.tree.inherited</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_tree2" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="hours" invisible="1"/>
                <field name="est_hours" widget="float_time" sum="Total Hours"/>
                <field name="amount" sum="Total Amount"/>
                <field name="total_amount" invisible="1"/>
            </field>
        </field>
    </record>

    <record id="account_task_kanban_view" model="ir.ui.view">
        <field name="name">project.task.kanban.inherited</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_kanban" />
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('o_kanban_record_bottom')]" position="after">
                <br/>
                <label><strong>Estimated Hour:</strong></label> 
                <field name="est_hours"/>
                <br/>
                <label><strong>Estimated Amount:</strong></label>
                <field name="amount"/>
                <!-- <label><strong>Total Amount:</strong></label>
                <field name="total_amount"/> -->
            </xpath>
        </field>
    </record>

    <record id="view_project_task_pivot_inherit" model="ir.ui.view">
        <field name="name">project.task.pivot.inherit</field>
            
        <field name="model">project.task</field>
            
        <field name="inherit_id" ref="project.view_project_task_pivot"/>
            
        <field name="arch" type="xml">
            <xpath expr="//field[@name='stage_id']" position="after">
                <field name="est_hours" string="Estimated Hours"/>
                <!-- <field name="amount" string="Estimated Amount"/> -->
            </xpath>
        </field>
    </record>

</odoo>