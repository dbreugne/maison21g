<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_backfill_create_invoices" model="ir.actions.server">
        <field name="name">Create Missing Invoices (m21_api backlog)</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_backfill_create_invoices()</field>
    </record>

</odoo>
