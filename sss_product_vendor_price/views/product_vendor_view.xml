<odoo>

    <record id="product_supplierinfo_list_view" model="ir.ui.view">
        <field name="name">product.supplierinfo.view</field>
        <field name="model">product.supplierinfo</field>
        <field name="arch" type="xml">
            <tree string="Vendor Information" multi_edit="1">
                    <field name="sequence" widget="handle"/>
                    <field name="name" readonly="1"/>
                    <field name="is_subcontractor"/>
                    <field name="product_id" readonly="1" optional="hide"
                        invisible="context.get('product_template_invisible_variant', False)"
                        groups="product.group_product_variant"
                        domain="[('product_tmpl_id', '=?', context.get('default_product_tmpl_id', False))]"
                        options="{'no_quick_create': True, 'no_create_edit' : True}"/>
                    <field name="product_tmpl_id" string="Product" readonly="1"
                        invisible="context.get('visible_product_tmpl_id', True)"/>
                    <field name="product_name" optional="hide"/>
                    <field name="product_code" optional="hide"/>
                    <field name="currency_id" groups="base.group_multi_currency"/>
                    <field name="date_start" optional="hide"/>
                    <field name="date_end" optional="hide"/>
                    <field name="company_id" readonly="1" groups="base.group_multi_company"/>
                    <field name="min_qty"/>
                    <field name="product_uom" groups="uom.group_uom"/>
                    <field name="price" string="Price"/>
                    <field name="delay" optional="hide"/>
                </tree>
           <!--  <tree>
                <field name="name"/>
                <field name="is_subcontractor"/>
            </tree> -->
        </field>
    </record>

    <record id="product_vendor_price_action_coupon_program" model="ir.actions.act_window">
        <field name="name">Product Vendor</field>
        <field name="res_model">product.supplierinfo</field>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="product_supplierinfo_list_view"/>
    </record>

    <menuitem id="menu_product_vendor_type_config" action="product_vendor_price_action_coupon_program" parent="sale.product_menu_catalog" name="Product Vendor Price" sequence="7"/>

</odoo>